using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pizza 
{
    /*
    public Pizza(string name, string kind, int size) : base(name, kind, size)
    {
        SetName(name);
        SetKind(kind);
        SetSize(size);
    }
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
